apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-temp-data-pvc
  namespace: dev
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azurefile-csi
  resources:
    requests:
      storage: 1Gi
