name: Incidents Service CI

on:
  push:
    branches: [develop, main]
    paths:
      - "Final-Capstone/services/incidents-service/**"
  pull_request:
    branches: [develop, main]
    paths:
      - "Final-Capstone/services/incidents-service/**"

permissions:
  contents: read
  packages: write
  security-events: write

jobs:
  ci:
    uses: Suwetha07/incident-Tracker/.github/workflows/ci.yaml@main
    with:
      image_name: "incidents-service"
      docker_context: "Final-Capstone/services/incidents-service"
    secrets: inherit
