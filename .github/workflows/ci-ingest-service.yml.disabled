name: Build Ingest Service

on:
  push:
    branches: [main, develop]
    paths:
      - 'Final-Capstone/services/ingest-service/**'
      - '.github/workflows/ci-ingest-service.yml'
      - '.github/workflows/ci.yaml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'Final-Capstone/services/ingest-service/**'
      - '.github/workflows/ci-ingest-service.yml'
      - '.github/workflows/ci.yaml'

permissions:
  contents: read
  packages: write
  security-events: write

jobs:
  build:
    uses: ./.github/workflows/ci.yaml
    permissions:
      contents: read
      packages: write
      security-events: write
    with:
      image_name: "ingest-service"
      docker_context: "Final-Capstone/services/ingest-service"
    secrets: inherit
