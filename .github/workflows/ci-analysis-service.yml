name: Analysis Service CI

on:
  push:
    branches: [develop, main]
    paths:
      - "Final-Capstone/services/analysis-service/**"
  pull_request:
    branches: [develop, main]
    paths:
      - "Final-Capstone/services/analysis-service/**"

permissions:
  contents: read
  packages: write
  security-events: write

jobs:
  ci:
    uses: Suwetha07/incident-Tracker/.github/workflows/ci.yaml@main
    with:
      image_name: "analysis-service"
      docker_context: "Final-Capstone/services/analysis-service"
    secrets: inherit
